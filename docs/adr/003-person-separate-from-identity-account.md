# ADR 003: Person is separate from identity account

Status: Accepted

A human can own many accounts and an account state does not define employment state. `Person`, `IdentityAccount` and reasoned `IdentityLink` records remain distinct to avoid unsafe lifecycle inference.
