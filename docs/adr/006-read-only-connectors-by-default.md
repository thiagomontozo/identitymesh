# ADR 006: Read-only connectors by default

Status: Accepted

All connectors begin with writes disabled. An administrator must enable writes and the connector must declare the exact capability before a mutating action can be planned.
